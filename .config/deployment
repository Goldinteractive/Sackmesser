DEPLOYMENT_FOLDER=./.deployment

# should git be used to tag the deployments?
USE_GIT=1

# should a db be used?
USE_DB=1

DUMP_FOLDER="$DEPLOYMENT_FOLDER/database/structure/tables"

source .env

DB_DEV_DATABASE=$DB_DATABASE
DB_DEV_PORT=$DB_PORT
DB_DEV_HOST=$DB_HOST
DB_DEV_USER=$DB_USERNAME
DB_DEV_PW=$DB_PASSWORD

# Document Root Path (usually . or ./public)
WEBROOT_PATH=./public

# Folders where uploaded files are.
### craft files
#DEPLOY_DATA_FOLDERS[0]="public/files"
#DEPLOY_DATA_FOLDERS[1]="craft/storage/files"
#DEPLOY_DATA_FOLDERS[2]="craft/storage/rebrand"
#DEPLOY_DATA_FOLDERS[3]="craft/storage/userphotos"

DEPLOY_STORAGE_FOLDER="storage"

# Folder where the data files should be backuped
DEPLOY_DATA_BACKUP_FOLDER="$DEPLOYMENT_FOLDER/data"

# binaries
MYSQL=mysql
MYSQLDUMP=mysqldump





